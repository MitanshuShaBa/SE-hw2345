name: Python Test

on: push

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          node-version: 3.7
      - name: Test
        run: |
          python ./src/test.py
